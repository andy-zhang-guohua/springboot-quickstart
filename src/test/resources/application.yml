server:
    port: 8080 #当前服务节点端口
    contextPath: /

spring:
  application:
    name: jpa-specification #当前应用的名称


logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
